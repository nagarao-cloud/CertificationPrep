# 00-START-HERE

Placeholder folder. Content generated as the study plan reaches it.

If this exam folder is ever bulk-generated (see `_scripts/README.md` at
the repo root), the first file written here should be `RUNBOOK.md` —
the discovered domain/task/sub-skill structure and coverage map
produced by `CLAUDE.md` §12 Step 0, before anything else is generated.
